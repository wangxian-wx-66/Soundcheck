# P0-C 收尾 + P0-D 打磨 + P1 杠精 Agent（2026-09-13 晚）

> 环境：本地 Windows + Node 22；mock 验证（单测 25/25、烟测 11/11、压测 7/7）+ 真 key 端到端 1 次
> 真实报告原始数据：`2026-09-13_P0C收尾_P1杠精_真key报告.json` | runId: 6956ad47-1b2e-473f-a35b-d8affb9180c9

## 一、本次交付内容

### P0-C① SSE 事件落盘 SQLite（G2 硬性验收项）
- 新增 `events` 表（run_id + seq 主键，带索引）；`emit()` 每条事件同步落库，落盘失败不影响在线推送
- 清理策略：孤儿事件（run 未落 reports 且超 1 小时）周期清理 + 总行数上限 60,000（≈1000 报告 × 2 倍余量）
- 重启回放：`getEvents`/`getStatus` 内存 miss 时回退 db；SSE 端点对被重启打断的 run 补发 `SERVER_RESTARTED` failed 事件（诚实告知，绝不静默挂死）
- **验证**：单测「模拟重启回放 + Last-Event-ID 增量」通过；烟测新增第 8 项（kill 服务 → 同 dataDir 重启 → 事件 19 条完整回放 + 状态恢复 done + 未知 run 404）

### P0-C② quota 降级接线（省额度保险丝）
- 服务启动自检 `quota`（不消耗额度；查询失败/未配置/SMOKE 不降级——降级必须是可核实判断）
- zhihu_search 余量 <20% → `quotaLow`：系统提示声明 + `executeTool` 硬拦截「标杆序之后的搜索」（L2 补强跳过），首次搜索与标杆序不受影响
- `/api/health` 暴露 `quotaLow` 字段
- **验证**：单测「quotaLow 下 L2 搜索被拒，上游仅 1 次真实调用，meta.note 声明降级」通过

### P0-C③ 10 并发压测（`npm run loadtest`，SMOKE+LLM_MOCK 零额度）
- 10 路同时 analyze（6 独立题 + 4 重复同题），`--max-old-space-size=768` 下验证
- **结果 7/7 通过（实测 2026-09-13）**：
  - 10/10 done 无失败，无超时崩溃
  - 闸门排队：4 路收到 queued 事件，最大位置 4（活跃上限 6 生效）
  - single-flight/缓存合并：10 路仅 2 个唯一检索词 → 上游真实调用 zhihu_search=2、question_answers=1
  - 内存水位 RSS 62MB ≪ 512MB 安全线（1G 容器余量充足）
  - 闸门完全复位（running=0 queued=0），压测后报告可回访
- **压测过程中发现并修复一个真 bug**：mock LLM 的 steps 数组全实例共享——10 路并发时只有第 1 路走完整工具链，其余 9 路零工具调用直接收尾，压测形同虚设。修复为「每个新对话首轮重置脚本」（lib/llm.mjs），这是 mock 层 bug 不影响生产链路，但此前所有依赖 mock 的并发验证都被高估
- MOCK_LATENCY_MS 环境变量：mock 检索延迟可调（压测用 300ms 模拟真实 0.7s 量级，让排队逻辑可被确定性触发）

### P0-D① benchmark chip 作者名
- question_answers 接口无作者字段（实测确认）→ 用搜索证据做 answer ID ↔ 作者名交叉匹配：命中显示 `社区序 #3 · 时光纪`；未命中诚实回退摘要截断（不伪造作者）
- **验证**：单测通过（mock 链路 53910077 → 时光纪；未匹配项回退截断）

### P0-D② 失败态重试按钮
- 研究台 failed 事件 → desk-foot 显示「重试本次分析」按钮，用表单现有输入（sessionStorage 持久）重新发起
- 重启打断（SERVER_RESTARTED）同样落失败态 → 可重试

### P1 杠精 Agent（双角色分歧呈现，G4 晋级项提前完成）
- **后端**（lib/pipeline.mjs）：review 模式主报告完成后，杠精 Agent 独立对抗审草稿——找茬 3 条 + 独立评级 + 与主审分歧点；复用主审已采集证据（真实评论样本 12 条上限），零额外检索额度；失败不拖垮主报告（cynic_failed 事件）
- **事件流**：cynic_start / cynic_done / cynic_failed，研究台杠精发言流实时渲染
- **前端**：研究台新增杠精面板（红色系视觉区隔，发言流逐步呈现找茬①②③ + 杠精评级/分歧数 chips）；战报卡新增第 4 区「评审团分歧」（主审评级 VS 杠精评级大字对照 + 分歧双栏 + 找茬 3 条带证据徽标）
- **验证**：单测 5 项（3 条找茬/真实评论佐证来源约束/radar 无杠精/失败隔离/草稿隐私——杠精结论不含草稿原文）+ 烟测 + 真实链路

## 二、真 key 端到端（2026-09-13）

| 项 | 值 |
|---|---|
| 模式 | review：真实问题「机器学习该怎么入门？」+ 转行者视角真实草稿（项目驱动路线） |
| 耗时 | **48.2s**（含杠精一次额外 LLM 调用；主审约 28s 量级 + 杠精约 20s） |
| 工具调用 | 6/6；question_answers 两次命中 qa_cache（0 消耗，9/13 实测缓存生效） |
| 事件 | 31 条完整：start → parse → 检索循环 → benchmark ×2 → final → **cynic_start → cynic_done** → done |
| 主审评级 | C（vs 9/13 首次 E2E 的 B——不同草稿） |
| 杠精评级 | C（与主审一致，但分歧点具体） |

**杠精质量抽查（真 key）**：3 条找茬全部 real 佐证，且全部针对草稿真实表述——
1. 「先 pandas 后 sklearn」是伪次序，文科生真实路径是稀里糊涂跟项目入门
2. 只回答了「先学什么」，没回答「今晚第一步敲哪一行」
3. 全文无翻车经历/版本/报错/数据集来源——评论区已备好「AI生成」的现成锤子

**分歧点质量（真 key，两条）**：
- resonance 评分：主审给 6（次序之争有共鸣基础）vs 杠精主张 3-4（真实评论共鸣点是「迷茫、从哪下手」，次序之争是答主自嗨）
- unique 增量保留：主审保留附警示 vs 杠精主张砍掉（「给负资产挂有争议标签，实际增量应为零，评级上限 C」）

「内部有分歧的评审团」叙事在真实链路成立——分歧具体、可验证、不是装饰。

## 三、消耗与额度

- 真 key 1 次：zhihu_search 上游 4 次（另 2 次缓存命中）+ question_answers 0 次（qa_cache 7 天 TTL 命中）+ DeepSeek 约 10 轮（含杠精翻倍）
- 启动 quota 自检 1 次（zhihu_search 余量充足 → quotaLow=false）

## 四、遗留与下一步

- 杠精与主审**串行**（主报告完成后才跑）——计划书原文「同管线并行」；串行实现更简单且能复用完整证据 + 主审结论算分歧，48s 总耗时可接受。若 Demo 需要更快可改为 parse 后即启动杠精（互不依赖部分）
- 前端双角色为上下布局（主审 feed 在上、杠精面板在下），非左右并列——760px 单栏设计下上下更稳
- 部署六步仍待执行（不阻塞，G1 门禁「线上可访问」项）
