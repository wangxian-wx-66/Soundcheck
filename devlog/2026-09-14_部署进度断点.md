# 部署进度存档（断点续传文档）· 2026-09-14 14:00 更新

> **用途**：任何新会话/新 agent 读这份文档即可精确接续部署。读完直接从「当前断点」继续，不要重做已完成步骤。
> 配套阅读：`docs/开麦评审官_开发计划.md`（v2.23）+ `devlog/2026-09-14_部署前置.md`

## 一、部署六步进度（总计划 §六）

| # | 步骤 | 状态 |
|---|---|---|
| 1 | Dockerfile + .dockerignore + pre-commit 钩子 | ✅ 完成 |
| 2 | Sealos 部署应用 | ✅ **完成——Pod Running，公网可访问** |
| 3 | configure_callback.mjs 登记回调 | 🔶 **当前断点（下一步执行）** |
| 4 | 官方项目页改回调地址 | ⏸ 待步骤 3（用户浏览器操作） |
| 5 | Sealos 注入三个 Secret | ✅ 已随部署填好 |
| 6 | 线上验证 + prewarm 预热 | 🔶 部分：健康/机会榜/真 key 端到端 ✅；OAuth 人肉验证 + 预热 ⏸ |

**步骤 2 补充（两个坑已修，详见第三节）**：镜像地址重复拼接 → 拆开填；/data 权限 CrashLoop → entrypoint chown 降权。

**线上验证已过项**：
- `GET /api/health`：`llm=deepseek-flash, zhihu=ready, quotaLow=false` ✅
- 机会榜：3 条 gap 58 + 二次缓存命中 ✅
- 参数校验 400 / OAuth 未登录 401 ✅
- **真 key 端到端分析**：review 模式跑完（rating B、6/6 检索、4 论点、杠精 3 条、诚实 note 完整、缓存修复后 L1 真实生效）✅
- 烟测 `--url` 8/10：两个 FAIL 是脚本按 mock 30s 超时判定的误报（真 key 链路 1-2 分钟），服务实际正常——该分析最终 done 已人工确认

## 二、当前断点（下一步从这里继续）

**步骤 3：本地跑回调登记脚本**（写回 hackathon.config.json）：

```powershell
node c:\Soundcheck\scripts\configure_callback.mjs --project-dir c:\Soundcheck --redirect-uri https://czhuqikmrvth.sealoshzh.site/auth/callback
```

**步骤 4（用户在浏览器操作）**：知乎官方项目提交页（zhihu.com/hackathon → 我的项目）→「知乎登录回调地址」改成 `https://czhuqikmrvth.sealoshzh.site/auth/callback`——**与脚本登记的必须一字不差**（协议/域名/路径/斜杠）。当前官方登记的还是 `http://127.0.0.1`。

**步骤 6 收尾**：
1. OAuth 登录全流程人肉验证（浏览器点「知乎登录」→ 授权 → 回跳 → 「我的创作」解锁）
2. 预热：`node scripts/prewarm.mjs --url https://czhuqikmrvth.sealoshzh.site`（真 key，3 个演示问题落缓存）
3. 烟测已跑过（8/10 + 真链路人工确认，无需重跑）

## 三、已解决的两个部署坑（修复方案已验证，勿重做）

### 坑 1：镜像地址重复拼接（ImagePullBackOff）

- Sealos 表单把「镜像仓库地址」+「镜像名」**强拼接**
- **正确填法**：仓库地址 `crpi-zz1n5apaevyna2z4.cn-beijing.personal.cr.aliyuncs.com` + 镜像名 `soundcheck/soundcheck:latest`（两边只出现一次域名）

### 坑 2：/data 权限导致 CrashLoopBackOff（崩在 db.mjs:68）

- 根因：Sealos 本地存储挂载点属主 root，容器 node 用户写不进
- **修复**：`docker-entrypoint.sh`（root 起飞 → chown /data → su node 降权运行）+ Dockerfile `ENTRYPOINT`（去掉 USER node）
- 修复镜像已推送并部署：digest sha256:a795f3e07f43c501...

## 四、关键信息速查

| 项 | 值 |
|---|---|
| **正确公网域名** | `https://czhuqikmrvth.sealoshzh.site` |
| 镜像地址 | `crpi-zz1n5apaevyna2z4.cn-beijing.personal.cr.aliyuncs.com/soundcheck/soundcheck:latest` |
| 镜像仓库凭证 | 阿里云账号 `aliyun4402429732` + 访问凭证密码（用户已知） |
| Sealos 配置 | 固定实例 × 1 · CPU 0.5 · 内存 1G · 端口 4173 · 本地存储 /data 1Gi |
| **回调地址（步骤 3-4 用）** | `https://czhuqikmrvth.sealoshzh.site/auth/callback` |
| OAuth 凭证 | App ID 399 / App Key 3269…6e7（完整值在 Sealos Secret 与用户环境变量，赛后重置） |
| 实名认证 | 未做（不实名只限充值；¥5 赠送 > 全程成本 ≈¥2.1，暂不处理） |
| 预热命令 | `node scripts/prewarm.mjs --url https://czhuqikmrvth.sealoshzh.site` |

## 五、部署后收尾清单（别忘）

1. ~~线上烟测~~ ✅ / OAuth 登录人肉验证（待回调登记后）
2. prewarm 预热（评委体验秒出）
3. **git push 全部未推代码**（Dockerfile/entrypoint/钩子/使用指南/v2.20-v2.23 全部改动 + 本文档都还没推！）
4. 更新 `docs/使用指南.md` 开头占位域名为 `https://czhuqikmrvth.sealoshzh.site`
5. devlog 归档部署完成 + 计划书 v2.24（G1「线上可访问」终于闭环）
6. 录演示视频 / 计划书终稿 / **9/15 10:00 提交**
7. 评审期 9/15-9/23 **冻结重部署**（emptyDir 会丢缓存，重部署后必须重跑 prewarm）

## 六、架构备注

- Sealos「本地存储」= emptyDir（**非 PVC**）：重部署丢缓存/报告/OAuth 会话，数据可再生，评审期冻结即可
- 镜像入口：docker-entrypoint.sh（root chown → su node）；非 root 直启也兼容
- agent 本机 TLS 对该 NLB 有干扰：**用 Node fetch 或用户浏览器访问**（PowerShell/curl 失败不代表服务故障）
- **域名易抄错**：`czhuqikmrvth`（c-z-z-h-u-q-i-k-m-r-v-t-h，OCR 曾多识别一个 z）
